name: memegenerator
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  #Service Locator
  get_it: ^7.2.0
  #State Management
  flutter_bloc: ^8.0.1
  #For handling equalities
  equatable: ^2.0.3
  #Functional Programming
  dartz: ^0.10.1
  #Remote Api and Network Testing
  cross_connectivity: ^3.0.5
  http: ^0.13.4
  #Local Caching
  shared_preferences: ^2.0.15
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:


  uses-material-design: true
